@*@using MimMVC.Migrations
@model IEnumerable<MimMVC.Models.WorkShop>

@{
    ViewData["Title"] = "Index";
}
<br />
<h4>Alle afregninger</h4>

<p>
	<a class="btn btn-success" asp-action="Create"><i class="fas fa-plus-square"></i>&nbsp;Opret ny</a>
</p>


<form asp-action="Index">
    <div class="input-group">
        <input name="searching" class="form-control" type="text" placeholder="Navn" />
        <div class="input-group-append">
            <span class="input-group-text red lighten-3" id="basic-text1">
                <i class="fas fa-search text-gary"
                   aria-hidden="true"></i>
            </span>
        </div>
    </div>
</form>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OutTime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Hours)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Paid)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FileName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reg)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Kontor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.InTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.OutTime)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Hours)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Paid)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FileName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Reg)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Kontor)
                </td>
				<td>
					<a class="btn btn-secondary " asp-action="Edit" asp-route-id="@item.Id"><i class="fas fa-edit"></i>&nbsp;Redigere</a>
					<a class="btn btn-danger " asp-action="Delete" asp-route-id="@item.Id"><i class="fas fa-trash-alt"></i>&nbsp;Slet</a>					
				</td>
            </tr>
        }
    </tbody>
</table>

@section Scripts
{
    <script>
        $(function () {
            $("basic-text1").click(function () {
                $("Form").submit();
            })
        })
    </script>
}*@
